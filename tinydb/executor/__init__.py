"""查询执行器子包：catalog / ddl / dml / select / aggregate / index_plan / checkpoint。

主类 `Executor` 在 Batch 8 填充（REQ-QE-001..011）。
"""

__all__: list[str] = ["Executor"]


class Executor:  # 占位，Batch 8 替换
    """执行器主类（占位，Batch 8 实现）。"""

    def __init__(self, *args: object, **kwargs: object) -> None:
        raise NotImplementedError("Executor 在 Batch 8 实现")
