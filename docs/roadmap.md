# TinyDB 路线图

> W1 占位文件。v0.2 延期项唯一真值源，在 Batch 12 填充（REWRITE-PENDING 4.4）。

## v0.1（当前）

嵌入式关系型数据库核心：类型系统、存储引擎、WAL 事务、B+ Tree 索引、SQL 解析、
Query Executor、CLI/REPL、Database 包装层。

## v0.2（候选，未排期）

- 多表 JOIN
- ALTER TABLE
- 视图 / 触发器 / 外键
- 并发控制
- 网络服务
